# App
APP_NAME="Homelab Notes+Tasks"
APP_ENV=dev
SECRET_KEY="change-me-please"
BASE_URL="http://localhost:8000"

# Database
DATABASE_URL="postgresql+psycopg://app:app@db:5432/app"

# Storage (attachments - optional; not enabled in UI yet)
STORAGE_BACKEND="local"   # local | s3
LOCAL_STORAGE_PATH="/data/uploads"

# Search (optional)
SEARCH_BACKEND="simple"   # simple | postgres_fts | meilisearch

# Jobs (optional)
JOBS_BACKEND="inline"     # inline | redis

# Email (future)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
